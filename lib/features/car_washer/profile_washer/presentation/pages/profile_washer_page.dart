import 'package:flutter/material.dart';

class ProfileWasherPage extends StatelessWidget {

  const ProfileWasherPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ProfileWasher Page'),
      ),
    );
  }

}
