import 'package:flutter/material.dart';

class WashersPage extends StatelessWidget {

  const WashersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Washers Page'),
      ),
    );
  }

}
