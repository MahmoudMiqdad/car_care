import 'package:flutter/material.dart';

class RateJobPage extends StatelessWidget {

  const RateJobPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('RateJob Page'),
      ),
    );
  }

}
